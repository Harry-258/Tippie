import {User} from "../util/types.js";

export async function getUser(uid: string): Promise<User> {
    try {

    } catch (error) {
        console.error("Error getting user info in services: " + error);
    }
}

export async function getUserTeam(uid: string): Promise<> {
    try {

    } catch (error) {
        console.error("Error getting user info in services: " + error);
    }
}

export async function assignUserTeam(uid: string) {
    try {

    } catch (error) {
        console.error("Error getting user info in services: " + error);
    }
}